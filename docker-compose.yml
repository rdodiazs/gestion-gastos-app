services:
    frontend:
        build: 
            dockerfile: Dockerfile
            context: ./frontend
        image: react-image:1.0
        container_name: react_cont
        ports:
            - "5173:5173"
        volumes:
            - ./frontend:/app
            - /app/node_modules
        stdin_open: true
        tty: true
